<?php
$headers = array(_("Name"));
$items = array();
foreach ($studiengaenge as $studiengang) {
    $items[] = array(
        'content' => array(
            $studiengang['name']
        ),
        'url' => URLHelper::getLink("?", array('user_id' => $studiengang['studiengang_id']))
    );
}
?>
<h1 style="text-align: center;"><?= _("Personal nach Studiengängen") ?></h1>

<?= $this->render_partial("zsb/partials/editable.php", compact("headers", "items")) ?>